// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @unique @default(uuid())
  firstName String
  lastName String 
  address String
  email String @unique
  phone String
  password String

  postedProducts Product[] @relation("postedBy")
}

model Product {
  id String @id @default(uuid())
  title String 
  categories String[]
  description String
  price Int
  rentPrice Int
  rentDuration String
  viewCount Int @default(0)
  
  purchaseInfo SoldProduct ? @relation("purchaseInfo", fields : [isProductBought] , references: [id])
  isProductBought String ? @unique

  rentInfo RentedProduct ? @relation("rentInfo", fields : [isProductRented] , references: [id])
  isProductRented String ? @unique

  postedByUser User  @relation("postedBy" , fields: [postedBy] , references: [id])
  postedBy String
}

model RentedProduct{
  id String @id @default(uuid()) 
  product Product ? @relation("rentInfo")
  productOwner String
  borrowedBy String
  // rentDuration String
}
model SoldProduct{
  id String @id @default(uuid()) 
  product Product ? @relation("purchaseInfo")
  productOwner String
  boughtBy String
}
